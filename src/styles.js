import {StyleSheet} from 'react-native';

export const topicItem = StyleSheet.create({
  container: {
    // backgroundColor:
    padding: 8,
    borderRadius: 6,
    margin: 5,
  },
  text: {
    fontWeight: 'bold',
    color: 'white',
    fontSize: 20,
  },
});
